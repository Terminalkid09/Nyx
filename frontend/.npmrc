# eslint@10 is newer than some plugins' declared peer ranges (e.g.
# eslint-plugin-jsx-a11y@6.10.2 declares eslint "^3 || ... || ^9"). The plugins
# work fine in practice; relax npm's strict peer resolution so `npm ci`
# succeeds on any machine and in CI without local overrides.
legacy-peer-deps=true
